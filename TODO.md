# TODO

- [x] Create premium UI pass for paywall funnel (Pricing, Paywall, UpiPremiumPopup, PremiumPaywall) and supporting utilities in `frontend/src/index.css`.

- [x] Extend `frontend/src/index.css` with premium utility classes/tokens (sheen, check rows, premium file input wrapper).
- [x] Update `frontend/src/pages/Pricing.jsx` for premium hierarchy/ribbon/feature list polish.
- [x] Update `frontend/src/pages/Paywall.jsx` for consistent premium card/steps/CTA sheen.
- [x] Update `frontend/src/pages/UpiPremiumPopup.jsx` for premium-styled inputs/status/QR block (visual-only).
- [x] Update `frontend/src/components/PremiumPaywall.jsx` to consistently use brand token (define `--brand-color` if needed).
- [x] Run `cd frontend && npm run build -s` and fix UI-only build errors.

