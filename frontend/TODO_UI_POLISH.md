# UI/UX Polish TODO (Frontend only)

- [x] Update `frontend/src/index.css` with premium focus/hover/typography refinements (no logic changes).
- [x] Update `frontend/src/components/Navbar.jsx` to improve spacing/active/hover/dropdown/mobile polish (no handler/route/auth changes).
- [x] Update `frontend/src/pages/Login.jsx` to modernize layout hierarchy and button/card visuals (no validation or submit logic changes).
- [x] Update `frontend/src/pages/Register.jsx` to match Login premium style while preserving OTP and validations.

- [x] Run `cd frontend; npm run build -s` and fix any UI-only build/lint errors. Build successful with no errors.

